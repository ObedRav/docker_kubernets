{
    "configurations": [
        {
            "name": "Docker Node.js Launch",
            "type": "docker",
            "request": "launch",
            "preLaunchTask": "docker-run: debug",
            "platform": "node",
            "node": {
                "package": "${workspaceFolder}/container_images/package.json",
                "localRoot": "${workspaceFolder}/container_images"
            }
        }
    ]
}